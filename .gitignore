# Dependencies
node_modules/

# Build output
dist/
.vite/
*.tsbuildinfo

# Logs
*.log

npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local

# Runtime data — case records + uploaded evidence (do NOT commit).
# `legal_sections.json` is the multi-act legal-section reference table
# (BNS + IPC + NDPS + POCSO + ...) extracted from the input PDFs and is
# committed alongside the codebase so the server can load it on every
# cold start; everything else under server/data/ stays ignored.
server/data/
!server/data/legal_sections.json

# Editor
.vscode/
.idea/

.vercel

# Vercel deploy tokens + helper scripts (NEVER commit)
.vercel-token
.auth-*.sh
.deploy-*.sh
.vercel-api.sh
.vercel-cmd.sh
deploy.out
deploy.err
.env*

server/secrets/

# Throwaway Vercel diagnostic scripts (not project code)
.check-*.py
.check-*.sh
.fetch-*.py


# docs/

# moved PHQ complaint dashboard out of this repo (lives in ~/Downloads)
phq-complaint-dashboard-migrated/
malkhana_sections_backup.json
.tmp/

# Local verification screenshots (kept on disk, not in repo)
case-property-detail-*.png
register-modal-*.png
